# Public release verification keys

Commit only the public GnuPG key here (`gotigin-ros-release.pub.asc`).

Generate keys and build signed downloads via
[docs/runbooks/release-packaging.md](../../../docs/runbooks/release-packaging.md).
Private material stays under `secrets/signing/` (gitignored).
