org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Flutter 3.44.6's Gradle plugin still uses the legacy Android extension. Keep
# these compatibility switches until the pinned Flutter SDK supports AGP 9's
# built-in Kotlin DSL end to end; removing them currently fails configuration.
android.newDsl=false
android.builtInKotlin=false
